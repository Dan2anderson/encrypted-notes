<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/tanlight"
    tools:context="com.example.encryptednotes.MainActivity">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never"
        android:background="@color/tanlight"
        android:itemBackground="@color/tanlight"
        android:panelFullBackground="@color/tanlight"
        android:theme="@style/CustomMenuItemStyle"
        />
</menu>