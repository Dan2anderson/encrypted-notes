<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/tanlight"
    tools:context="com.bedrock.encryptednotes.MainActivity">
    <item
        android:id="@+id/import_notes"
        android:orderInCategory="100"
        android:title="@string/import_notes"
        app:showAsAction="never"
        android:background="@color/tanlight"
        android:itemBackground="@color/tanlight"
        android:panelFullBackground="@color/tanlight"
        android:theme="@style/CustomMenuItemStyle"
        /> <item
        android:id="@+id/export_notes"
        android:orderInCategory="100"
        android:title="@string/export_notes"
        app:showAsAction="never"
        android:background="@color/tanlight"
        android:itemBackground="@color/tanlight"
        android:panelFullBackground="@color/tanlight"
        android:theme="@style/CustomMenuItemStyle"
        />
</menu>